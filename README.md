# PHP 8.5 con MongoDB

Scaricare i pacchetti per MongoDB:

```bash
composer require mongodb/mongodb
```

Come avviare il webserver + PHP:

```bash
php -S 0.0.0.0:8000
```

Come collegare VS Code a MongoDB:

```bash
mongodb://mongo:27017
```
